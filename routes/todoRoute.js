const express = require('express');
const todosController = require('../controllers/todos');

const router = express.Router();

// router.get('/todos', todosController.getTasks);

// router.get('/newTodo', todosController.getCreateTodoPage);

// router.post('/newTodo', todosController.createTodo);

// router.post('/updateTodo/:todo', todosController.updateTodo);

// router.put('/completeTodo/:todo', todosController.completeTodo);

// router.delete('/deleteTodo/:todo', todosController.deleteTodo);

module.exports = router;